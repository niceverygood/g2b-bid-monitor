# g2b attachment worker 환경변수
# 이 파일을 .env 로 복사한 뒤 값을 채우고 `docker compose up -d` 로 실행

# 필수
SUPABASE_URL=https://tzpsmijmlcpanbjnzagv.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOi...service_role_jwt...

# 선택 (기본값 유지 권장)
MIN_SCORE=70
POLL_INTERVAL_MS=300000
